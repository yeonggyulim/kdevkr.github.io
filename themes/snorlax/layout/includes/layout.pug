doctype html
html(lang=config.language)
    head
        include head
    body
        include nav
        section.section.is-paddingless
            div.columns
                div.column
                    block header
                    if body
                        div!= body
                    else
                        block content
        include footer
        script(src="/js/index.bundle.js")
