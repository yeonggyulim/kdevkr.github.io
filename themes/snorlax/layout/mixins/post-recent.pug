section.widget
    h3.heading=theme.widgets.recent_post
    ul.recent-posts
        each post in site.posts.sort('date', -1).limit(5).toArray()
            li.recent-posts__item
                a(href=url_for(post.path))= post.title || 'Untitled'
